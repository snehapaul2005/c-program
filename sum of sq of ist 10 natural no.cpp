#include<stdio.h>
#include<math.h>
int main()
{
	int a;
	a=pow(1,2)+pow(2,2)+pow(3,2)+pow(4,2)+pow(5,2)+pow(6,2)+pow(7,2)+pow(8,2)+pow(9,2)+pow(10,2);
	printf("The sum of the square of first 10 natural numbers is %d",a);
	return 0;
}
